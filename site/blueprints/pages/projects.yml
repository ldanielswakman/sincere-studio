title: Projects
preset: page

pages:
  template: project
files: false

sections:

  projects:
    headline: Projects
    type: pages
    layout: cardlets
    create: project
    template: project
    limit: 1000
    text: "{{page.title}}"
    info: "{{page.year}}"
    image:
      back: white
      query: page.featuredimage.toFile
      cover: true

  text:
    type: fields
    fields:
      title:
        label: Title
        type:  text
      text:
        label: Text
        type:  textarea