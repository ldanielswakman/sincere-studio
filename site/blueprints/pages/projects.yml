title: Projects
preset: page
icon: grid

pages:
  template: project
files: false

sections:

  projects:
    extends: fields/project-tiles
    create: project
    template: project
    # limit: 1000

  content:
    type: fields
    fields:
      title:
        label: Title
        type:  text
      featured:
        label: Featured Projects
        type: pages
        query: site.find('projects').children
        min: 0
        max: 6
        layout: cardlets