title: CV
pages: false
files: true

tabs:
  general:
    label: Content
    icon: page
    fields:

      work_xp:
        extends: fields/cv-section
        label: Work Experience

      work_projects:
        label: Projects
        type: pages
        query: site.find('projects').children.listed

      cv_graph:
        label: CV
        type: files
        multiple: false
        layout: cards
        size: huge
        image:
          ratio: 10/1
          back: white

      education:
        extends: fields/cv-section
        label: Education

      personal:
        extends: fields/cv-section
        label: Personal

      languages:
        extends: fields/cv-section
        label: Languages

      skills:
        extends: fields/cv-section
        label: Skills

      interests:
        extends: fields/cv-section
        label: Interests

      preferences:
        extends: fields/cv-section
        label: Preferences

      text:
        label: Text
        type:  textarea
        width: 1/2

      pdf_file:
        label: PDF File
        type:  files
        multiple: false
        width: 1/2
  
  files: tabs/files
